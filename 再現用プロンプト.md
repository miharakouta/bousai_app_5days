# 防災情報Webアプリを再現するための一括実装プロンプト

あなたは熟練したフルスタック開発者です。以下の仕様をすべて満たす、動作確認可能な防災情報Webアプリを、現在の開発環境に合わせて実装してください。実装途中で確認質問はせず、最後まで完成させてください。既存プロジェクトがある場合は、既存の技術・構成・デザインを尊重し、必要な範囲だけ変更してください。

## 1. アプリの目的

青森市の住民が、避難所、現在の天気、気象警報・注意報、過去の災害地点、行政からの指示・発信を確認できるWebアプリを作る。管理者は避難所を登録し、住民向けの発信を登録できる。

## 2. 技術・構成

- サーバーサイドレンダリングを基本とするFlaskアプリにする。
- Python、Flask 3.0系、Jinja2、JSONファイル永続化を使用する。
- エントリーポイントはプロジェクト直下の `app.py` とする。
- テンプレートは `bousai_app/templates/`、データは `bousai_app/data/` に配置する。
- `requirements.txt` に必要な依存関係を記載する。最低限Flaskを含める。
- `vercel.json` を用意し、VercelのPythonランタイムから `app.py` を実行できるようにする。
- 地図はCDNのLeaflet 1.9.4とOpenStreetMapを使う。
- 日本語を標準表示言語にし、共通ヘッダーの `English` ボタンで主要な日本語表示を英語へ切り替える。再度押すと日本語へ戻す。
- 現在時刻はJSTで扱い、画面には `YYYY年MM月DD日 HH:MM` 形式で表示する。
- JSONはUTF-8、`ensure_ascii=False`、インデント2で保存する。
- 外部入力をテンプレートへ表示するときはJinja2の自動エスケープを利用する。

## 3. 共通UI

全ページに共通のヘッダーとナビゲーションを表示する。

- 青色のヘッダーに、左からハンバーガーメニュー、タイトル「防災アプリ」、右側の `English` 切替ボタンを配置する。
- ハンバーガーメニューは初期状態で閉じる。クリックで開閉し、外側クリックまたはEscapeキーで閉じる。
- 開閉ボタンには `aria-expanded`、`aria-controls`、適切な `aria-label` を設定する。
- メニュー項目はホーム、避難所検索、問い合わせ、ログインまたはログアウトとする。
- 管理者ログイン中だけ、避難所登録と指示・発信ボードをメニューに表示する。
- 画面幅600px以下でも崩れないレスポンシブレイアウトにする。
- ホームは白いコンテンツ領域、薄い境界線、控えめな影、青系見出しを基本にする。
- ボタン、フォーム、リンク、状態表示、見出し、メニュー項目、`title`、`placeholder`、ARIAラベルなど、利用者が見る主要な文字列を英語切替の対象にする。

## 4. ルートと機能

### ホーム `/`

次のセクションをこの順番で表示する。

1. **避難所マップ**
   - Leafletで地図を初期化し、青森市役所付近 `[40.8220, 140.7474]`、ズーム12を初期表示にする。
   - OpenStreetMapタイルを表示する。
   - `latitude` と `longitude` が数値として有効な避難所だけをマーカー表示し、ポップアップに避難所名を表示する。
   - マーカーがある場合は全マーカーが見えるように自動調整する。

2. **過去の災害レイヤー**
   - `historical_disasters.json` の発生地点を地図上に表示する。
   - 年、種類、地域の3つのセレクトボックスを用意し、初期値はすべて「すべて」にする。
   - 絞り込み変更時は該当地点だけを再描画し、発生地点数を表示する。
   - 災害マーカーのポップアップに年、種類、日付、地域、説明を表示する。

3. **テスト実行ボタン**
   - 初期表示は「テスト実行」。クリックすると現在地点（青森市役所）、オレンジ色の避難経路、警報エリア、注意報エリアを地図へ追加する。
   - 警報エリアに地図を移動し、ページ背景をオレンジ系の警告色にする。
   - ボタンを「テスト解除」に変える。再クリックでテスト用レイヤーとテスト由来の背景色を解除し、ボタンを元へ戻す。
   - 実際の警報が発表されている場合は、テスト解除後も警告背景を維持する。

4. **青森市 現在の天気**
   - `/api/weather_forecast` を初回表示時に取得する。
   - 天気概況、気温、体感温度、湿度、風速、観測時刻、更新時刻を表示する。
   - 取得失敗時は「現在の天気を取得できませんでした」と表示する。
   - 10分（600,000ミリ秒）ごとに自動更新する。

5. **青森市 気象警報・注意報**
   - `/api/weather_warnings` を初回表示時に取得し、10分ごとに自動更新する。
   - 地域名、警報・注意報名、状態（「発表」または「継続」）、データ取得時刻、気象庁発表時刻を表示する。
   - 情報がなければ「現在、警報・注意報は発表されていません」と表示する。
   - 取得失敗時はエラーを表示し、実際の警告背景は解除する。ただしテスト中はテスト背景を維持する。
   - 警報名に「警報」が含まれる情報がある場合、テスト中でなければページ背景を警告色にする。

6. **通知履歴**
   - JSONの配列を新しいものから表示する。
   - 気象通知には通知日時、気象庁発表時刻、警報一覧、発表なし表示を含める。
   - 避難所登録と指示・発信の通知にはタイトル、内容、通知日時、「更新」バッジを表示する。
   - 状況に応じて「緊急」「警報」「注意報」「発表なし」「更新」のバッジを表示する。
   - 履歴が空なら「通知履歴はありません」と表示する。

7. **指示・発信**
   - `target` が「住民」のデータだけを新しい順に表示する。
   - 内容、避難先（設定時）、発信日時、「発信中」バッジを表示する。
   - 発信が空なら「現在、住民向けの発信はありません」と表示する。

8. **最終ログイン時刻**
   - ログイン済みの場合はホーム下部に最終ログイン時刻を表示する。

### ログイン `/login`

- 「住民として利用する」「管理者として利用する」の2つの送信ボタンを表示する。
- 共通パスワード入力欄を表示し、再現版のパスワードは `123` とする。
- 成功時は `logged_in`、`username`、`role`、`last_login_time` をセッションへ保存する。
- `role` は `resident` または `admin` とし、管理者用機能は `role == admin` の場合だけ許可する。
- ロールが不正なら「利用者を選択してください。」、パスワードが不正なら「パスワードが正しくありません。」と表示する。
- `next` パラメータを受け取り、同一ホストのHTTP/HTTPS URLだけへリダイレクトする。外部URLや不正な値はホームへ戻し、オープンリダイレクトを防ぐ。
- ログイン済みでログイン画面へアクセスした場合は、安全な `next` またはホームへリダイレクトする。
- 旧URL `/admin_login` は共通ログイン画面へリダイレクトする。
- `/logout` はセッションを全消去し、ホームへ戻す。

### 避難所関連

- `/shelter_search` は「登録されている避難所を確認できます。」と「全施設一覧」リンクを表示する。
- `/all_shelters` は登録済み避難所の名前を一覧表示する。空の場合は「該当する避難所が見つかりませんでした」と表示し、検索画面とホームへのリンクを表示する。
- `/search_results?district=地区名` は `district` の完全一致で絞り込む。未指定なら全件を返す。避難所名を表示する。
- `/shelters?district=地区名` は一致する避難所のJSON配列を返す。0件なら `{"error":"No shelters found"}` をHTTP 404で返す。

### 管理者用避難所登録 `/shelter_register`

- 未ログインまたは住民ロールの場合は管理者ログインへ誘導する。
- 避難所名だけを必須入力とし、地区・緯度・経度を勝手に必須化しない。
- 空欄なら「避難所名を入力してください。」と表示し、保存しない。
- 登録時は既存の最大ID + 1を採番し、少なくとも `id` と `name` を保存する。
- 保存成功時は「避難所を登録しました。」を表示し、通知履歴へ避難所登録通知を先頭追加する。
- 保存失敗時は追加したデータを元に戻し、「避難所情報を保存できませんでした。」を表示する。

### 管理者用指示・発信ボード `/board`

- 管理者ロールだけがアクセスできる。その他は管理者ログインへ誘導する。
- 発信内容を必須、避難先を任意で入力できる。
- 空欄なら「発信内容を入力してください。」と表示し、保存しない。
- 発信データには `id`、`target: "住民"`、`content`、`shelter`、`status: "発信中"`、`created_at`、`updated_at` を保存する。
- 新しいデータを先頭へ追加し、通知履歴にも「指示・発信」として先頭追加する。
- 発信一覧には内容と避難先を表示し、空の場合は「登録されている指示はありません。」と表示する。
- 保存失敗時は追加した発信を元に戻し、「発信内容を保存できませんでした。」を表示する。

### 問い合わせ `/contact`

次の情報を表示する。

- 問い合わせホーム
- 「防災アプリに関するお問い合わせはこちらからご連絡ください。」
- 防災アプリ運営窓口
- 受付時間：平日 9:00〜17:00
- メール：`contact@example.com`（mailtoリンク）

## 5. JSONデータ

指定ファイルが存在しない、読めない、または壊れている場合でも、該当データを空配列としてアプリを起動できるようにする。ファイル保存時は既存のデータを壊さないよう、失敗時にメモリ上の追加分をロールバックする。

### `shelters.json`

```json
[
  {
    "id": 1,
    "name": "青森市立長島小学校",
    "district": "中央部",
    "latitude": 40.8219,
    "longitude": 140.7408
  }
]
```

`district`、`latitude`、`longitude` がない避難所も許容する。ただし座標がない避難所は地図に表示しない。

### `instructions.json`

各要素に `id`、`target`、`content`、`shelter`、`status`、`created_at`、`updated_at` を持たせる。ホームには `target` が「住民」の要素だけを表示する。

### `notification_history.json`

各要素に `timestamp`、`area_name`、必要に応じて `title`、`content`、`event_type`、`report_time`、`warnings`、`warning_count`、`has_emergency`、`has_warning`、`has_advisory` を持たせる。新しい操作通知は配列の先頭へ追加する。

### `historical_disasters.json`

各要素に `id`、`year`、`date`、`type`、`location`、`latitude`、`longitude`、`magnitude`、`scale`、`description` を持たせる。地図のフィルターとポップアップに使用する。

## 6. 外部API

### 気象庁の警報・注意報

- 対象地域は青森県・青森市とする。
- 都道府県コードは `020000`、市区町村コードは `1420500`、地域名は「青森市」とする。
- URLは `https://www.jma.go.jp/bosai/warning/data/r8/020000.json` とする。
- HTTPタイムアウトは10秒程度にする。
- 新形式JSON配列の各要素から `warning.class20Items` を探し、`areaCode == "1420500"` の地域だけを処理する。
- `kinds` のうち、`status` が「発表」または「継続」で、コードが存在するものを抽出する。
- コード重複を除去する。
- 代表的なコード名は次の通りにする。`00: 解除`、`02: 暴風雪警報`、`03: レベル3大雨警報`、`04: 洪水警報`、`05: 暴風警報`、`06: 大雪警報`、`10: レベル2大雨注意報`、`14: 雷注意報`、`15: 強風注意報`、`18: 洪水注意報`、`20: 濃霧注意報`。
- 未知のコードはコード番号を含む「不明な警報・注意報」として表示する。
- `/api/weather_warnings` の正常レスポンスは `{area_name, warnings, report_time, last_fetch_time}` とする。
- 取得・解析失敗時は `error: true`、`report_time: "取得失敗"`、空の警報配列を返す。

### Open-Meteoの現在天気

- 青森市役所付近の緯度経度 `40.8220, 140.7474` を使う。
- `https://api.open-meteo.com/v1/forecast` の `current` から気温、相対湿度、体感温度、天気コード、風速を取得する。
- timezoneは `Asia/Tokyo`、タイムアウトは10秒程度にする。
- `/api/weather_forecast` は地域名、気温、体感温度、湿度、風速、天気概況、観測時刻、取得時刻を返す。
- 取得失敗時は `error: true` を返す。

## 7. 完成条件と検証

実装後、利用可能なテスト、lint、型チェックを実行し、失敗があれば修正してから完了する。最低限、Flaskのテストクライアントなどで次を検証する。

1. `/`、`/login`、`/shelter_search`、`/contact` がHTTP 200を返す。
2. 未ログインで `/board` と `/shelter_register` にアクセスすると管理者ログインへ誘導される。
3. パスワード `123` で住民・管理者を選択でき、管理者ログイン後に管理者用2画面が開ける。
4. 避難所登録でJSONと通知履歴が更新され、空欄では保存されない。
5. 指示・発信でJSONと通知履歴が更新され、ホームに住民向け発信が表示される。
6. `/shelters` の地区完全一致、0件時HTTP 404 JSONが正しく動く。
7. ホームのLeaflet地図、座標付き避難所、過去災害フィルター、テスト実行・解除、気象API初回取得、10分更新、警告背景が動く。
8. JSONファイルが空、存在しない、または壊れていてもアプリが起動する。
9. `next` に外部URLを指定しても外部サイトへリダイレクトしない。
10. 起動方法、必要な依存関係、テスト方法をREADMEまたはプロジェクト内ドキュメントに記載する。

最後に、実装したファイル、起動コマンド、検証結果を簡潔に報告してください。
