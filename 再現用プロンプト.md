# 防災アプリ再現用プロンプト

あなたは熟練したフルスタック開発者です。以下の仕様をすべて満たす、動作確認可能な防災情報 Web アプリを、現在の開発環境に合わせた適切な技術で実装してください。実装途中で確認質問はせず、最後まで完成させてください。既存プロジェクトがある場合は、その構成・技術・デザイン規約を尊重し、不要な全面改修は避けてください。

## 目的

青森市の住民が避難所、気象警報・注意報、行政からの避難指示を確認でき、管理者が避難所と住民向け発信を登録できる、シンプルで実用的な防災アプリを作る。

## 技術要件

- サーバーサイドレンダリングを基本とする Web アプリにする。
- 現行版と同等に、Flask 3.0 系、Jinja2 テンプレート、JSON ファイル永続化で実装してよい。
- アプリのエントリーポイントは `app.py` とし、テンプレートは `bousai_app/templates/`、データは `bousai_app/data/` に置く。
- `requirements.txt` に必要な依存関係を記載する。
- Vercel で `app.py` を実行できる構成にする。
- 日本語を標準表示言語とし、ページ全体を英語表示へ切り替えられるようにする。
- 現在時刻は日本時間（JST）で `YYYY年MM月DD日 HH:MM` 表示にする。

## 画面とルート

### 1. ホーム `/`

共通ヘッダーの下に、次の順で表示する。

1. **避難所マップ**
   - Leaflet 1.9.4 と OpenStreetMap のタイルを使用する。
   - 青森市役所付近（緯度 `40.8220`、経度 `140.7474`）を中心、ズーム 12 で初期表示する。
   - 緯度・経度が有効な避難所だけをマーカー表示し、マーカーのポップアップに避難所名を表示する。
   - マーカーがある場合は全マーカーが見えるように自動調整する。

2. **テスト実行ボタン**
   - 初期表示は「テスト実行」。押すと地図上に現在地点（青森市役所）、オレンジ色の避難経路、警報エリア、注意報エリアを表示する。
   - 地図を警報エリアに合わせ、ページ背景を警告色（オレンジ）に変更する。
   - ボタンを「テスト解除」に変え、再度押すとテスト用レイヤーと背景色を解除する。
   - 実際の警報がある場合は、テスト解除後も警告背景を維持する。

3. **青森市 気象警報・注意報**
   - `/api/weather_warnings` を初回表示時に取得する。
   - 10 分（600,000 ms）ごとに自動更新する。
   - 地域名、警報・注意報名、状態（「発表」「継続」）、データ取得時刻、気象庁発表時刻を表示する。
   - 警報・注意報がなければ「現在、警報・注意報は発表されていません」と表示する。
   - 取得失敗時はエラー表示を行い、実際の警告背景は解除する。
   - 「警報」を含む情報があるときはページ背景を警告色にする。

4. **通知履歴**
   - 新しいものから順に表示する。
   - 気象情報の通知には通知日時、気象庁発表時刻、警報一覧、発表なし表示を含める。
   - 避難所登録・指示発信の通知にはタイトル、内容、通知日時、「更新」バッジを表示する。
   - 通知種別に応じて「緊急」「警報」「注意報」「発表なし」「更新」のバッジを表示する。
   - 履歴がなければ「通知履歴はありません」と表示する。

5. **指示・発信**
   - `target` が「住民」の発信だけを新しい順に表示する。
   - 内容、避難先（設定されている場合）、発信日時、発信中バッジを表示する。
   - 発信がなければ空状態を表示する。

6. ログイン済みなら最終ログイン時間を表示する。

### 2. ログイン `/login`

- 「住民として利用する」「管理者として利用する」の 2 つから選択する。
- パスワード入力は不要。選択したロールをセッションに保存する。
- `logged_in`、`username`、`role`、`last_login_time` をセッションに保存する。
- `next` パラメータを受け取れるが、同一ホストの HTTP/HTTPS URL だけを許可し、オープンリダイレクトを防止する。
- 不正なロールの場合は「利用者を選択してください」と表示する。
- ログイン済みなら安全な `next` またはホームへリダイレクトする。
- 旧 URL `/admin_login` はログイン選択画面へリダイレクトする。
- `/logout` でセッションを全消去し、ホームへ戻す。

### 3. 避難所検索 `/shelter_search`

- 「登録されている避難所を確認できます」と表示する。
- `/all_shelters` への「全施設一覧」リンクを提供する。

### 4. 避難所一覧 `/all_shelters`

- 登録済み避難所を一覧表示する。
- 少なくとも避難所名を表示する。
- 結果がなければ「該当する避難所が見つかりませんでした」と表示し、検索画面・ホームへのリンクを提供する。

### 5. 検索結果 `/search_results?district=地区名`

- `district` が指定された場合は地区が完全一致する避難所だけを返す。
- 指定がなければ全件を返す。
- 一覧表示は避難所名を含める。

### 6. JSON API `/shelters?district=地区名`

- 検索条件に一致した避難所配列を JSON で返す。
- 0 件の場合は `{"error": "No shelters found"}` を HTTP 404 で返す。

### 7. 管理者用避難所登録 `/shelter_register`

- 管理者ロールのみアクセス可能。未ログインまたは住民ロールは管理者ログインへ誘導する。
- 避難所名（必須）の入力フォームと登録ボタンを表示する。
- 空欄なら「避難所名を入力してください」と表示し、保存しない。
- 登録時は既存最大 ID + 1 の ID を採番し、避難所名を保存する。
- 登録成功時は「避難所を登録しました」と表示し、通知履歴にも登録内容を追加する。
- 保存失敗時は追加したデータを元に戻し、「避難所情報を保存できませんでした」と表示する。
- この画面の基本仕様は、避難所名だけを登録する現行動作を維持する。登録時に地区・緯度・経度を勝手に必須化しない。

### 8. 管理者用指示・発信ボード `/board`

- 管理者ロールのみアクセス可能。
- 住民向け発信内容（必須）と避難先（任意）を入力できる。
- 空欄なら「発信内容を入力してください」と表示し、保存しない。
- 発信データには ID、`target: "住民"`、内容、避難先、`status: "発信中"`、作成日時、更新日時を保存する。
- 新しい発信を先頭に追加し、通知履歴にも「指示・発信」として追加する。
- 発信一覧には内容と避難先を表形式などで表示する。
- 発信がなければ「登録されている指示はありません」と表示する。
- 保存失敗時は追加したデータを元に戻し、エラーを表示する。

### 9. 問い合わせ `/contact`

- 問い合わせホーム、説明文、運営窓口、受付時間「平日 9:00〜17:00」、メール `contact@example.com` を表示する。

## データ形式

JSON ファイルを UTF-8、インデント 2、非 ASCII 文字を保持して保存する。ファイルが存在しない、または壊れている場合は空配列で起動できるようにする。

### `shelters.json`

```json
[
  {
    "id": 1,
    "name": "青森市立長島小学校",
    "district": "中央部",
    "latitude": 40.8219,
    "longitude": 140.7408
  }
]
```

地区・座標が存在しないデータも許容し、地図からは除外する。

### `instructions.json`

各要素に `id`、`target`、`content`、`shelter`、`status`、`created_at`、`updated_at` を持たせる。ホームには `target` が「住民」のものだけを表示する。

### `notification_history.json`

各要素に `timestamp`、`area_name`、必要に応じて `title`、`content`、`event_type`、`report_time`、`warnings`、`warning_count`、`has_emergency`、`has_warning`、`has_advisory` を持たせる。新しい操作通知は先頭に追加する。

## 気象庁 API

- 対象は青森県・青森市。
- 都道府県コードは `020000`、市区町村コードは `1420500`、地域名は「青森市」とする。
- 次の URL から取得する。
  `https://www.jma.go.jp/bosai/warning/data/r8/020000.json`
- タイムアウトは 10 秒程度にする。
- 新形式 JSON 配列の各要素から `warning.class20Items` を探し、`areaCode == "1420500"` の地域だけを対象にする。
- `kinds` のうち `status` が「発表」または「継続」で、コードがあるものを抽出する。
- コード重複は除去する。
- 主なコード名は以下を使用する。`00: 解除`、`03: レベル3大雨警報`、`04: 洪水警報`、`05: 暴風警報`、`06: 大雪警報`、`10: レベル2大雨注意報`、`14: 雷注意報`、`15: 強風注意報`、`18: 洪水注意報`、`20: 濃霧注意報`。未知のコードはコード番号付きの「不明な警報・注意報」として表示する。
- レスポンスは `{ area_name, warnings, report_time, last_fetch_time }` とし、取得・解析失敗時は `error: true`、`report_time: "取得失敗"`、空の警報配列を返す。

## 共通 UI / デザイン

- 全ページで共通ヘッダーを使用する。
- ヘッダーは青色、左にハンバーガーメニュー、中央寄りに「防災アプリ」、右に English 切替ボタンを置く。
- メニューは初期状態で閉じ、ボタンで開閉する。外側クリックまたは Escape キーで閉じる。
- メニュー項目はホーム、避難所検索、管理者のみ避難所登録・指示発信ボード、問い合わせ、ログインまたはログアウト。
- キーボード操作可能にし、開閉状態を `aria-expanded` と `aria-label` で表す。
- 全体は白いコンテンツカード、薄い境界線、控えめな影、青系の見出しで構成する。
- 画面幅 600px 以下でも崩れないレスポンシブレイアウトにする。
- 余白、文字サイズ、地図高さをモバイル向けに調整する。
- 英語切替では主要な見出し、ボタン、状態文、ラベル、ARIA ラベルを英訳する。切替ボタンは「日本語」に変え、再度押すと日本語に戻す。
- ユーザー入力はテンプレート側で適切にエスケープする。

## 完成条件

次をすべて満たすこと。

1. アプリを起動して `/`、`/login`、`/shelter_search`、`/contact` を HTTP 200 で開ける。
2. 未ログインで `/board` と `/shelter_register` にアクセスすると管理者ログインへ誘導される。
3. ログイン画面で住民・管理者を選べ、管理者ログイン後に `/board` と `/shelter_register` が開ける。
4. 管理者が避難所を登録すると JSON と通知履歴が更新される。
5. 管理者が発信すると JSON と通知履歴が更新され、ホームに住民向け発信が表示される。
6. `/shelters` の地区絞り込み、0 件時 404 JSON が正しく動く。
7. ホームの地図、テスト実行・解除、気象情報初回取得・10 分更新、警告背景が動く。
8. JSON が空または壊れていてもアプリが起動する。
9. `next` に外部 URL を指定しても外部サイトへリダイレクトしない。
10. 起動方法と必要な依存関係を README またはプロジェクト内ドキュメントに記載し、テストまたは Flask のテストクライアントで主要ルートを検証する。

実装後、利用可能なテスト・ lint・型チェックを実行し、失敗があれば修正してから完了としてください。
